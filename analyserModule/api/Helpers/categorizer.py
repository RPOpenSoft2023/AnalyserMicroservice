import pandas as pd
import re
import math

# for row in range(df.shape[0]):
#     msg = df['Particulars'][row]
#     if (msg == msg):    # is str
#         string = re.split(r"[-/;,.\s]", msg)
#         newStr = ""
#         for item in string:
#             newStr += item.lower()

#     # print(newStr)
#     df.at[row, 'updatedParticular'] = newStr
#     # break
